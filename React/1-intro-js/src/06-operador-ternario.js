const average = 5.9;

const status = average >= 5.5 ? 'Approved' : 'Failured';

console.log(`Resultado: ${status}`);

const a = 5;
const b = 8;
const c = 3;

let max = a > b ? a : b;
max = max > c ? max : c;

console.log(`The maximum value is ${max}`);